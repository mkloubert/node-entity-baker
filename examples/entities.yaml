---
namespace: MarcelJoachimKloubert.Database
entities:
  User:
    table: users
    columns:
      id:
        id: true
        auto: true
        type: int32
      name: string
      email: string
      context: json
